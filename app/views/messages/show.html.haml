%script{type: "text/javascript", src: "/javascript/messages.js"}

.well
  - if @message.title
    %h1
      = @message.title

  = render partial: 'shared/message', locals: {message: @message, full: true}