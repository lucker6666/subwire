%h1
  = t('helpers.sign_up')

= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  %fieldset
    %div{:class => "clearfix"}
      = f.text_field :name, class: 'text_field', placeholder: t('helpers.name'), maxlength: 30

    %div{:class => "clearfix"}
      = f.text_field :email, class: 'text_field', placeholder: t('helpers.email')

    %div{:class => "clearfix"}
      = f.password_field :password, class: 'text_field', placeholder: t('helpers.password')

    %div{:class => "clearfix"}
      = f.password_field :password_confirmation, class: 'text_field', placeholder: t('helpers.password_confirmation')

    %br/
    = f.submit t("helpers.sign_up"), class: "btn btn-success"
    = link_to t("helpers.back"), new_session_path(resource_name), class: "btn btn-inverse"
